diff --git a/src/auth/session.ts b/src/auth/session.ts
index a1b2c3d..d4e5f6a 100644
--- a/src/auth/session.ts
+++ b/src/auth/session.ts
@@ -10,7 +10,12 @@ export interface Session {
 }
 
 export function createSession(userId: string): Session {
-  const token = crypto.randomUUID();
+  const token = crypto.randomBytes(32).toString('hex');
   const expiresAt = Date.now() + 24 * 60 * 60 * 1000;
-  return { userId, token, expiresAt };
+  return {
+    userId,
+    token,
+    expiresAt,
+    issuedAt: Date.now(),
+  };
 }
diff --git a/src/utils/legacy-helpers.ts b/src/utils/legacy-helpers.ts
deleted file mode 100644
index 9f8e7d6..0000000
--- a/src/utils/legacy-helpers.ts
+++ /dev/null
@@ -1,12 +0,0 @@
-export function formatDateLegacy(d: Date): string {
-  return d.toISOString();
-}
-
-export function parseDateLegacy(s: string): Date {
-  return new Date(s);
-}
-
-export const LEGACY_TIMEZONE = 'UTC';
-
-export function isLeapYear(year: number): boolean {
-  return (year % 4 === 0 && year % 100 !== 0) || year % 400 === 0;
-}
diff --git a/src/api/routes.ts b/src/api/handlers.ts
similarity index 92%
rename from src/api/routes.ts
rename to src/api/handlers.ts
index 1234567..7654321 100644
--- a/src/api/routes.ts
+++ b/src/api/handlers.ts
@@ -1,5 +1,6 @@
 import { Router } from 'express';
+import { authenticate } from '../auth/session.ts';
 
 export const router = Router();
 
-router.get('/health', (_req, res) => res.json({ status: 'ok' }));
+router.get('/health', authenticate, (_req, res) => res.json({ status: 'ok' }));
diff --git a/src/db/migrations/0001_initial.ts b/src/db/migrations/0001_initial.ts
new file mode 100644
index 0000000..abcdef0
--- /dev/null
+++ b/src/db/migrations/0001_initial.ts
@@ -0,0 +1,18 @@
+import type { Migration } from '../types.ts';
+
+export const migration: Migration = {
+  id: '0001_initial',
+  up: async (db) => {
+    await db.schema.createTable('users', (t) => {
+      t.uuid('id').primaryKey();
+      t.string('email').unique().notNullable();
+      t.timestamp('created_at').defaultTo(db.fn.now());
+    });
+  },
+  down: async (db) => {
+    await db.schema.dropTable('users');
+  },
+};
+
+export default migration;
diff --git a/assets/logo.png b/assets/logo.png
new file mode 100644
index 0000000..1a2b3c4
Binary files /dev/null and b/assets/logo.png differ
