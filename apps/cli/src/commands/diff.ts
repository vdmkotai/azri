// SPDX-License-Identifier: Apache-2.0
// Copyright (c) 2026 Azri contributors

export function runDiff(args: string[]): Promise<number> {
  console.log('azri diff — local diff explainer mode');
  console.log('coming soon (T34 will implement this)');
  if (args.length > 0) console.log('Args received:', args.join(' '));
  return 0;
}
